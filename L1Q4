#include <stdio.h>

int main(){

        int a;
        puts("TOTAL DOS PEDIDOS:");
        scanf("%d", &a);
        a = (0.10 * a) + a;
        printf("TOTAL COM GARÇOM:%d\n", a );
        
        return 0;
}
